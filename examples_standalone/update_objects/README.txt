This example is intended to show how SCONS can be
"tricked" at begining of development to accept object
files as up to date.

This is contrary to SCONS default behaviour where it
assumes that anything which is not marked in it's own
database as uptodate is not uptodate.
