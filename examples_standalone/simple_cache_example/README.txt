This example shows how to use the scons cache feature.

If you want to remove the scons cache then issue:
	rm -rf mycache

to run scons without the cache feature:
	scons --cache-disable
to show the cache:
	scons --cache-show
